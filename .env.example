# Copy these placeholder names into your existing local secrets configuration.
# Never commit a populated .env file.
APIFY_API_TOKEN=apify_api_REPLACE_WITH_YOUR_FULL_TOKEN
APIFY_ACTOR_ID=username/actor-name
# Optional: needed only for authenticated-user or explicitly private GitHub discovery.
GITHUB_TOKEN=github_pat_REPLACE_WITH_A_FINE_GRAINED_READ_ONLY_TOKEN
